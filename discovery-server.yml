server:
  port: 8761

eureka:
  instance:
    hostname: discovery-server.spring-petclinic-staging.svc.cluster.local
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/